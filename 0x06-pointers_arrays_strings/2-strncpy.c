#include "main.h"

/**
* _strncpy - function that copies a string.
* @dest: parameter1
* @src: parametr2
* @n: an integer
* Return: string
*/
char *_strncpy(char *dest, char *src, int n)
{
	strncpy(dest, src, n)
	return (dest);
}
